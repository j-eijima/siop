.build/
.swiftpm/
.DS_Store
xcuserdata/
# Coding-agent configuration, kept local. The project's requirements and
# conventions live in README.md / README.ja.md instead.
CLAUDE.md
.claude/settings.local.json
# Generated by XcodeGen from project.yml
*.xcodeproj
*.xcresult
ios/SIOPApp/Info.plist
# Gradle
.gradle/
build/
.tls/
# Terraform: providers, and state that holds this deployment's details. The
# lock file is committed, so every apply uses the same provider versions.
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
